<div class="{{ styles.root}} {{class}}">
    <input class="{{ styles.input }}" type="{{ type }}" name="{{ name }}" placeholder=" " />
    <label for={{ name }} class="{{ styles.label }}">{{ label }}</label>
</div>